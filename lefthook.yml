# Git hooks managed by lefthook
# Run: lefthook install
#
# Extends shared hooks from .catalyst/tooling — add project-specific
# overrides below (they merge with the shared config).

extends:
  - .catalyst/tooling/shared/configs/lefthook.yml
